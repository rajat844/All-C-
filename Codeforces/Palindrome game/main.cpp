#include <iostream>
#include <string>

using namespace std;

void winner(string sequence){
    
}

int main() {
    int t;
    cin >> t;
    while (t--) {
        int length;
        cin >> length;
        string sequence;
        for(int i = 0; i<length;i++){
            char temp;
            cin>>temp;
            sequence.push_back(temp);
        }
        void winner(string sequence);
    }
    return 0;
}